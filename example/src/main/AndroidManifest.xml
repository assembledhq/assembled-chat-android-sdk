<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AssembledChatExample"
        android:usesCleartextTraffic="true">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AssembledChatExample">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ViewExampleActivity"
            android:label="@string/view_example"
            android:theme="@style/Theme.AssembledChatExample" />

        <activity
            android:name=".FragmentExampleActivity"
            android:label="@string/fragment_example"
            android:theme="@style/Theme.AssembledChatExample" />

        <activity
            android:name=".ComposeExampleActivity"
            android:label="@string/compose_example"
            android:theme="@style/Theme.AssembledChatExample" />

    </application>

</manifest>
